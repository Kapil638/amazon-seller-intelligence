name: Backend database CI

# 12B.2A.1 — greenfield addition; no CI existed in this repository before.
#
# Uses only a disposable, ephemeral PostgreSQL *service container* scoped to
# each job's lifetime — never reachable outside the job, never the
# developer's configured database (which does not exist in this environment
# at all). No deployment, publishing, or production credentials are used or
# required anywhere in this workflow.

on:
  workflow_dispatch: {}
  pull_request:
    paths:
      - "apps/api/**"
      - ".github/workflows/backend-database-ci.yml"

jobs:
  fresh-install-migration:
    name: Fresh PostgreSQL — zero-to-head migration
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_fresh
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_fresh
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade head (from empty database)
        working-directory: apps/api
        run: uv run alembic upgrade head
      - name: Confirm single head and expected revision
        working-directory: apps/api
        run: |
          uv run alembic heads | tee /tmp/heads.txt
          grep -q "0019_amazon_ads_foundation (head)" /tmp/heads.txt
          uv run alembic current | tee /tmp/current.txt
          grep -q "0019_amazon_ads_foundation" /tmp/current.txt

  existing-database-upgrade:
    name: Existing PostgreSQL (0008) — upgrade to 0009
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_existing
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_existing
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade to 0008 (simulates an existing pre-12B.2A database)
        working-directory: apps/api
        run: uv run alembic upgrade 0008_amazon_oauth_states
      - name: alembic upgrade to 0009 (the real 12B.2A upgrade path)
        working-directory: apps/api
        run: uv run alembic upgrade 0009_amazon_seller_identity
      - name: Confirm final revision
        working-directory: apps/api
        run: uv run alembic current | grep -q "0009_amazon_seller_identity"

  existing-database-upgrade-0010:
    name: Existing PostgreSQL (0009) — upgrade to 0010
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_existing_0010
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_existing_0010
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade to 0009 (simulates an existing pre-12B.3B database)
        working-directory: apps/api
        run: uv run alembic upgrade 0009_amazon_seller_identity
      - name: alembic upgrade to 0010 (the real 12B.3B upgrade path)
        working-directory: apps/api
        run: uv run alembic upgrade 0010_amazon_seller_listings
      - name: Confirm final revision
        working-directory: apps/api
        run: uv run alembic current | grep -q "0010_amazon_seller_listings"

  existing-database-upgrade-0011:
    name: Existing PostgreSQL (0010) — upgrade to 0011
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_existing_0011
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_existing_0011
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade to 0010 (simulates an existing pre-12B.3G database)
        working-directory: apps/api
        run: uv run alembic upgrade 0010_amazon_seller_listings
      - name: alembic upgrade to 0011 (the real 12B.3G upgrade path)
        working-directory: apps/api
        run: uv run alembic upgrade 0011_listings_job_lifecycle
      - name: Confirm final revision
        working-directory: apps/api
        run: uv run alembic current | grep -q "0011_listings_job_lifecycle"
      # This job proves the plain CLI upgrade path a real deployment
      # actually runs. The deeper behavioral proofs this migration needs
      # — existing-data preservation across the upgrade, a genuine queued
      # row with nullable started_at, the widened active-state partial
      # unique index, terminal historical rows surviving untouched, and
      # the downgrade's transactional all-or-nothing refusal when
      # incompatible rows exist — live in `tests/postgres/test_disposable_
      # postgres_listings_job_lifecycle_concurrency.py`, already exercised
      # by the `postgres-identity-concurrency` job below (`pytest tests/
      # postgres/`) rather than duplicated here as raw shell/SQL.

  existing-database-upgrade-0012:
    name: Existing PostgreSQL (0011) — upgrade to 0012
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_existing_0012
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_existing_0012
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade to 0011 (simulates an existing pre-12B.4B database)
        working-directory: apps/api
        run: uv run alembic upgrade 0011_listings_job_lifecycle
      - name: alembic upgrade to 0012 (the real 12B.4B upgrade path)
        working-directory: apps/api
        run: uv run alembic upgrade 0012_orders_foundation
      - name: Confirm final revision
        working-directory: apps/api
        run: uv run alembic current | grep -q "0012_orders_foundation"
      # As with the 0011 job above: the deeper behavioral proofs (existing-
      # data preservation, deterministic counter backfill, the coarser
      # orders-scope partial unique index, cross-participation/cross-
      # organization provenance rejection, downgrade refusal when Orders
      # data exists) live in `tests/postgres/test_disposable_postgres_
      # orders_migration.py`, exercised by the `postgres-identity-
      # concurrency` job below (`pytest tests/postgres/`) rather than
      # duplicated here as raw shell/SQL.

  existing-database-upgrade-0013:
    name: Existing PostgreSQL (0012) — upgrade to 0013
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_existing_0013
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_existing_0013
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade to 0012 (simulates an existing pre-remediation database)
        working-directory: apps/api
        run: uv run alembic upgrade 0012_orders_foundation
      - name: alembic upgrade to 0013 (the real 12B.4D remediation upgrade path)
        working-directory: apps/api
        run: uv run alembic upgrade 0013_orders_durable_pagination
      - name: Confirm final revision
        working-directory: apps/api
        run: uv run alembic current | grep -q "0013_orders_durable_pagination"
      # As with the 0011/0012 jobs above: the deeper behavioral proof
      # (existing pre-0013 Orders/run data preserved across the upgrade,
      # the three new columns existing and starting NULL, the orders-only
      # pagination-scope check constraint, and the downgrade's refusal
      # while a pagination token is in flight) lives in `tests/postgres/
      # test_disposable_postgres_orders_migration.py`, exercised by the
      # `postgres-identity-concurrency` job below (`pytest tests/
      # postgres/`) rather than duplicated here as raw shell/SQL.

  existing-database-upgrade-0014:
    name: Existing PostgreSQL (0013) — upgrade to 0014
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_existing_0014
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_existing_0014
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade to 0013 (simulates an existing pre-12B.6A database)
        working-directory: apps/api
        run: uv run alembic upgrade 0013_orders_durable_pagination
      - name: alembic upgrade to 0014 (the real 12B.6A upgrade path)
        working-directory: apps/api
        run: uv run alembic upgrade 0014_sales_traffic_foundation
      - name: Confirm final revision
        working-directory: apps/api
        run: uv run alembic current | grep -q "0014_sales_traffic_foundation"
      # As with the 0011/0012/0013 jobs above: the deeper behavioral proof
      # (existing pre-0014 run data preserved across the upgrade, the
      # seven new report-lifecycle columns existing and starting NULL,
      # the sales-and-traffic-only scope check constraints, the new
      # fact/checkpoint tables' schema, the active-scope partial unique
      # index under real concurrency-relevant constraints, and the
      # downgrade's refusal while any sales-and-traffic data exists)
      # lives in `tests/postgres/test_disposable_postgres_sales_traffic_
      # migration.py`, exercised by the `postgres-identity-concurrency`
      # job below (`pytest tests/postgres/`) rather than duplicated here
      # as raw shell/SQL.

  existing-database-upgrade-0015:
    name: Existing PostgreSQL (0014) — upgrade to 0015
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_existing_0015
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_existing_0015
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade to 0014 (simulates an existing pre-fix database)
        working-directory: apps/api
        run: uv run alembic upgrade 0014_sales_traffic_foundation
      - name: alembic upgrade to 0015 (fix/ingestion-worker-runtime-availability's own upgrade path)
        working-directory: apps/api
        run: uv run alembic upgrade 0015_worker_heartbeats
      - name: Confirm final revision
        working-directory: apps/api
        run: uv run alembic current | grep -q "0015_worker_heartbeats"
      # Deeper behavioral proof (expected table/CHECK-constraint schema,
      # a clean drop-only downgrade, and concurrent first-heartbeat
      # writes for the same worker_type under real concurrency) lives in
      # `tests/postgres/test_disposable_postgres_worker_heartbeats.py`,
      # exercised by the `postgres-identity-concurrency` job below
      # (`pytest tests/postgres/`) rather than duplicated here as raw
      # shell/SQL.

  existing-database-upgrade-0016:
    name: Existing PostgreSQL (0015) — upgrade to 0016
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_existing_0016
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_existing_0016
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade to 0015 (simulates an existing pre-12B.6B database, already carrying the runtime-availability fix)
        working-directory: apps/api
        run: uv run alembic upgrade 0015_worker_heartbeats
      - name: alembic upgrade to 0016 (the real 12B.6B upgrade path)
        working-directory: apps/api
        run: uv run alembic upgrade 0016_inventory_foundation
      - name: Confirm final revision
        working-directory: apps/api
        run: uv run alembic current | grep -q "0016_inventory_foundation"
      # As with the 0011/0012/0013/0014/0015 jobs above: the deeper
      # behavioral proof (existing pre-0016 run data preserved across the
      # upgrade, the widened run_type CHECK, the inventory-only scope
      # check constraint, the new current-state/observation tables'
      # schema, the active-scope partial unique index and claim path
      # under real concurrency, the corrected same-day-multiple-
      # observations grain, composite-FK provenance, and the downgrade's
      # refusal while any inventory data exists) lives in `tests/
      # postgres/test_disposable_postgres_inventory_migration.py`,
      # exercised by the `postgres-identity-concurrency` job below
      # (`pytest tests/postgres/`) rather than duplicated here as raw
      # shell/SQL.

  existing-database-upgrade-0017:
    name: Existing PostgreSQL (0016) — upgrade to 0017
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_existing_0017
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_existing_0017
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade to 0016 (simulates an existing pre-fix database, already carrying Inventory's own schema)
        working-directory: apps/api
        run: uv run alembic upgrade 0016_inventory_foundation
      - name: alembic upgrade to 0017 (fix/inventory-heartbeat-check-constraint's own upgrade path)
        working-directory: apps/api
        run: uv run alembic upgrade 0017_inventory_heartbeat
      - name: Confirm final revision
        working-directory: apps/api
        run: uv run alembic current | grep -q "0017_inventory_heartbeat"
      # The deeper behavioral proof (an 'inventory' heartbeat rejected
      # before this migration and accepted after it, real PostgreSQL
      # CHECK enforcement rather than only the application-layer
      # TypeError, and the downgrade's refusal while an 'inventory' row
      # still exists) lives in `tests/postgres/
      # test_disposable_postgres_worker_heartbeats.py`, exercised by the
      # `postgres-identity-concurrency` job below (`pytest tests/
      # postgres/`) rather than duplicated here as raw shell/SQL.

  existing-database-upgrade-0018:
    name: Existing PostgreSQL (0017) — upgrade to 0018
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_existing_0018
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_existing_0018
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade to 0017 (simulates an existing pre-pilot-deployment-ewise database, already carrying the inventory heartbeat fix)
        working-directory: apps/api
        run: uv run alembic upgrade 0017_inventory_heartbeat
      - name: alembic upgrade to 0018 (pilot-deployment-ewise correction 1's own upgrade path)
        working-directory: apps/api
        run: uv run alembic upgrade 0018_amazon_encrypted_secrets
      - name: Confirm final revision
        working-directory: apps/api
        run: uv run alembic current | grep -q "0018_amazon_encrypted_secrets"
      # The deeper behavioral proof (expected ciphertext-only column
      # shape, the reference primary key rejecting a duplicate insert,
      # raw nonce/ciphertext bytes round-tripping exactly, and a clean
      # drop-only downgrade) lives in `tests/postgres/
      # test_disposable_postgres_amazon_encrypted_secrets.py`, exercised
      # by the `postgres-identity-concurrency` job below (`pytest tests/
      # postgres/`) rather than duplicated here as raw shell/SQL.

  existing-database-upgrade-0019:
    name: Existing PostgreSQL (0018) — upgrade to 0019
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_existing_0019
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_existing_0019
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: alembic upgrade to 0018 (simulates an existing pre-Ads-foundation database)
        working-directory: apps/api
        run: uv run alembic upgrade 0018_amazon_encrypted_secrets
      - name: alembic upgrade to 0019 (the Amazon Ads read-only foundation's own upgrade path)
        working-directory: apps/api
        run: uv run alembic upgrade 0019_amazon_ads_foundation
      - name: Confirm final revision
        working-directory: apps/api
        run: uv run alembic current | grep -q "0019_amazon_ads_foundation"
      # The deeper behavioral proof (all 12 new Ads tables' schema, no
      # pre-existing table/row touched by the upgrade, and the downgrade's
      # refusal-when-populated plus a clean round trip when empty) lives
      # in `tests/postgres/test_disposable_postgres_amazon_ads_migration.py`,
      # exercised by the `postgres-identity-concurrency` job below (`pytest
      # tests/postgres/`) rather than duplicated here as raw shell/SQL.

  postgres-identity-concurrency:
    name: PostgreSQL — OAuth identity concurrency validation
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci-only-password
          POSTGRES_DB: asi_ci_concurrency
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      # Deliberately NOT the application DATABASE_URL — this job never sets
      # that variable, so the disposable-Postgres guard's "resembles the
      # configured database" check trivially passes (there is no configured
      # database in this job at all).
      ASI_ALLOW_DISPOSABLE_POSTGRES: "1"
      POSTGRES_DISPOSABLE_TEST_URL: postgresql://postgres:ci-only-password@localhost:5432/asi_ci_concurrency
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: Run disposable-PostgreSQL deployment and concurrency suite
        working-directory: apps/api
        run: uv run pytest tests/postgres/ -v

  backend-test-suite-and-drift-check:
    name: Backend test suite + migration/model drift check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv
      - name: Install backend dependencies
        working-directory: apps/api
        run: uv sync
      - name: Run backend test suite (SQLite, offline)
        working-directory: apps/api
        run: uv run pytest -q
      - name: Migration/model drift check (no database required)
        working-directory: apps/api
        run: uv run pytest tests/test_migration_chain_matches_orm_metadata.py -v
